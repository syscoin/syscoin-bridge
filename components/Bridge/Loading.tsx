import { Box } from "@mui/material";

const BridgeLoading = () => {
  return (
    <Box
      sx={{
        mt: 5,
        display: "flex",
        flexDirection: "column",
        minWidth: "20rem",
        width: "50%",
      }}
    >
      Loading
    </Box>
  );
};

export default BridgeLoading;
