import { Box } from "@mui/material";

const BridgeV3Loading = () => {
  return (
    <Box
      sx={{
        mt: 5,
        display: "flex",
        flexDirection: "column",
        minWidth: "20rem",
        width: "50%",
      }}
    >
      Loading
    </Box>
  );
};

export default BridgeV3Loading;
